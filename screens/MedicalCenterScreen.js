import React from 'react';
import ScreenWraper from '../components/ScreenWraper';
export default function SicknessScreen({ navigation, route }) {
    return (
        <ScreenWraper>

        </ScreenWraper>
    );
};